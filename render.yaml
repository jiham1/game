services:
  - type: web
    name: spell-craft-tactics
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.16.0
      - key: DATABASE_URL
        sync: false # User will provide this from Neon
